# Editor / OS cruft
*.swp
*.*~
.DS_Store
*.pyc
.vscode
.vscode_ext_*.ts
.idea
.opencode/
*.orig
*.bak

# Build outputs. bin/ also covers the deployed LSP server at bin/server/,
# and bin/obj catch leftover .NET output dirs from the removed F# tree.
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
# Stale .NET build output from the removed F# tree (the client repo ignores it too).
artifacts/

# Misc local artifacts
*.bz2
*.cwv
build-cache/
